# 2026-07-09

## 依赖更新（#53）
- 根据 CNB #53 issue 的自动检测报告，更新了后端 Python 依赖：
  - `redis` 8.0.0 → 8.0.1
  - `ruff` 0.15.17 → 0.15.20
  - `sentry-sdk` 2.62.0 → 2.64.0
  - `setuptools` 82.0.1 → 83.0.0
  - `uvicorn` 0.49.0 → 0.50.0（实际解析为 0.51.0）
- 修改文件：`backend/pyproject.toml`、`backend/uv.lock`
- 验证：ruff check 通过，337/338 测试通过（1 个失败是已有的 iprule 表缺失问题）

## 新增编码规则
- 创建 `.codebuddy/rules/code.mdc`，规定编码授权铁律：无显式授权禁止编码，默认单次授权
- 将编码授权铁律写入 `/root/.codebuddy/CODEBUDDY.md` 第六章第3条，确保通过 `<always_applied_user_rules>` 注入每次对话

## uv 工具链更新方案
- CNB 开发环境 `.cnb/Dockerfile`：在 `apt-get install` 后追加 `uv self update`，覆盖上游镜像旧版本（不用 `pip` 因为 uv 不一定通过 pip 安装）
- 生产 `Dockerfile`：已通过 `pip install uv` 自动获取最新版，无需改动

## deps-check.sh 改造：全流程跑通
- `detect_backend_updates()`：Python tomllib 解析 uv.lock TOML；`uv lock --upgrade` 输出 >/dev/null 防污染
- `fetch_changelogs()`：GitHub Releases → Compare API → PyPI changelog URL 三级降级
- `ai_analysis()` / `create_issue()`：stdin 管道 `jq -Rs` + `curl -d @-` 避 ARG_MAX
- Issue 正文：只保留报告 + AI 结论，changelog 原文不写入 issue
- 验证：全流程跑通，Issue #56 创建成功
